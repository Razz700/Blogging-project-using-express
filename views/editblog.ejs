<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Blog</title>
    <%-include('./partials/style.ejs')%>
</head>
<body>
    <%-include('./partials/header.ejs') %>
    <div class="main-addblog">
        <h2>Edit your Blog Here</h2>
        <form method="post" action="/updateblog?blogId=<%=blogData._id%>">
            <input type="text" value=<%=blogData.title%> name="title" placeholder="Enter Blog title here">
            <br><br>
            <textarea name="body" placeholder="Enter blog body here"><%=blogData.body%></textarea>
            <br><br>
            <input type="submit" value="Update">
            <% if(message){ %>
                <p><%= message%></p> 
             <% } %>
        </form>
    </div> 
</body>
</html>