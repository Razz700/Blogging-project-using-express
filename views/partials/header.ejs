<div class="header">
<p class="title">
    MicroBlog
</p>
<div class="tabs">
    <div class="tab"><a href="/">Home</a></div>
    <% if(isAuthenticated){ %>
    <div class="tab"><a href="/myblog">MyBlogs</a></div>
    <%  } %>
    <div class="logout">
        <% if(isAuthenticated){ %>
        <a href="/logout">Logout</a>
        <%  }else{ %>
            <a href="/login">Login</a>
            <%  } %>
    </div>
 </div>
</div>